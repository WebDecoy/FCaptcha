# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Environment
.env
.env.*
!.env.example

# Build outputs
*.exe
fcaptcha-server
server-go/server
server-go/server-go

# Logs
*.log

# OS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
*.swo

# Test coverage
coverage/
.coverage
htmlcov/

# Internal planning docs. The AI-agent-detection PRD was published here once and
# pulled again (1d31f97) because it enumerates unshipped weaknesses — an
# adversary reading it gets a prioritized worklist against the current release.
# Ignored wholesale so a `git add -A` cannot republish one by accident.
#
# Public documentation belongs in README.md / INSTALLATION.md, not here. If a
# public docs/ site is ever wanted, narrow this to `docs/PRD-*.md` rather than
# deleting the rule.
docs/

# Bench harness: replay output and captured-corpus scratch. The corpus itself
# (bench/corpus/) IS committed — it is the measurement baseline, and a benchmark
# whose inputs are not in the repo cannot be reproduced or argued with.
bench/*.json
bench/test-results/
